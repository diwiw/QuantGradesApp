{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Config: Linux Debug",
            "type": "shell",
            "command": "cmake --preset linux-debug",
            "group": "build"
        },
        {
            "label": "Build: Linux Debug (All)",
            "type": "shell",
            "command": "cmake --build --preset build-linux-debug --parallel",
            "dependsOn": [
                "Config: Linux Debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Admin: Clean Build",
            "type": "shell",
            "command": "rm -rf build/",
            "group": "build"
        }
    ]
}
